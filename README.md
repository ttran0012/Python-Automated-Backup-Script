# Python-Project

os: This built-in library allows you to interact with the operating system, such as creating directories, navigating the file system, and checking if a path is a file or directory.

shutil: Another built-in library, shutil provides utilities for copying files and directories, which is essential for the backup process.

datetime: This built-in library is used to generate timestamps, helping to create unique backup directories.

schedule (optional): If you want to include scheduling within the script, you can use the schedule library. It’s not built-in, so you would need to install it via pip install schedule.
